function control_Loading_initialize(loadingAnimation, hiddenObject, dataObject){
	return command_Loading_initialize(loadingAnimation, hiddenObject, dataObject);
}

function control_Loading_finish(loading, data){
	command_Loading_finish(loading, data);
}
